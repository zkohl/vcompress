import XCTest
@testable import vcompress

/// Placeholder test to ensure the test target compiles.
/// Real tests will be added in subsequent tasks.
final class PlaceholderTests: XCTestCase {
    func test_placeholder() {
        // This test exists only to ensure the test target compiles.
        XCTAssertTrue(true)
    }
}
